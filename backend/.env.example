# Backend HTTP port
BACKEND_PORT=8787

# Required for admin login endpoint (/api/auth/login)
ADMIN_PASSWORD=change-this-password

# Secret used to sign API tokens
TOKEN_SECRET=replace-with-a-long-random-secret

# Allowed browser origin(s). Use * for local dev only.
CORS_ORIGIN=*
